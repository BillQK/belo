import Admin from "./admin";
import Dev from "./dev";

const Mod = () => {
  // Search bar -> render this for dev and admin
  // fetch all the users data and display it
  // display user data -> render this for dev and admind
  // display privilege data -> render this for dev only
  // add buttons to grant/provoke admin and delete user
  // render with icon when they're admin
  return <Admin />;
};

export default Mod;
